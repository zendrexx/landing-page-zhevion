{
  "name": "zhevion-content",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply zhevion-content --local",
    "db:migrate": "wrangler d1 migrations apply zhevion-content --remote",
    "cf-typegen": "wrangler types --env-interface ContentEnv"
  },
  "devDependencies": {
    "wrangler": "^4.114.0",
    "typescript": "^5.7.3"
  }
}
